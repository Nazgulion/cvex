# Copy to .env and replace the password before running Docker Compose.
POSTGRES_PASSWORD=replace-with-a-long-url-safe-password
CVEX_UID=1000
CVEX_GID=1000

# Optional. An API key allows faster NVD synchronization.
NVD_API_KEY=
